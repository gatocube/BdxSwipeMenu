In AI assistant development the user should be able to see changes ASAP and test them from different devices

So when you made changes - deploy the project locally and only after that you run tests.

By default we host our local deployments in ~/wwww/$PROJECT_NAME folder.
Check env vars for deployment website port, path and url

If local deployment takes longer than 30s - you should notify the user about it