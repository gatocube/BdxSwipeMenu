import { ComingSoon } from '../components/ComingSoon'

export function DocsPage() {
    return (
        <ComingSoon
            emoji="📖"
            description="Full API documentation, usage guides, and interactive examples — covering the new declarative state API, presets, and wrapper mode."
        />
    )
}
