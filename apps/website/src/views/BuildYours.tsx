import { ComingSoon } from '../components/ComingSoon'

export function BuildYoursPage() {
    return (
        <ComingSoon
            emoji="🛠️"
            description="A visual configurator to design your own BdxSwipeMenu — pick actions, arrange chains, customize colors, and export ready-to-use code."
            color="violet"
        />
    )
}
